version: '3.1'

services:
  bookmanagerdb:
    image: mongo

  bookmanager.api:
    image: ${DOCKER_REGISTRY-}bookmanagerapi
    build:
      context: .
      dockerfile: src/Services/BookManager/BookManager.API/Dockerfile

volumes:
  mongo_data: